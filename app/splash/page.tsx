import { SplashPageClient } from "./SplashPageClient";

export const metadata = {
  title: "Welcome to All Property Link",
  description: "Kenya's most reliable marketplace for properties, short-term stays, fundis, and service providers.",
};

export default function SplashPage() {
  return <SplashPageClient />;
}